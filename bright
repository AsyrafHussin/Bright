#!/usr/bin/php
<?php

// include command
require __DIR__.'/Core/Command.php';

$command = $argv[1];

switch ($command) {
    case 'serve':
        Core\Command::serve((isset($argv[2])) ? $argv[2] : null);
        break;

    case 'list':
        Core\Command::listCommand();
        break;    
    
    default:
        Core\Command::commandNotFound($command);
        break;
}
?>